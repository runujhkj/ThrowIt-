using UnityEngine;
using UnityEngine.UI;

// UI.Text.text example
//
// A Space keypress changes the message shown on the screen.
// Two messages are used.
//
// Inside Awake a Canvas and Text are created.

public class PartCount : MonoBehaviour
{
    /*private PlayerInventoryController pic;
    private Text text;

    void Awake()
    {
        pic = GameObject.FindGameObjectWithTag("Player").GetComponent<PlayerInventoryController>();
        text = this.GetComponent<Text>();
    }

    void Update()
    {
        //Debug.LogFormat("Text.text: {0}\nVPartsLeft: {1}\nvPartTotal: {2}", text.text, pic.VPartsLeft(), pic.vPartTotal);
        text.text = pic.VPartsCollected().ToString() + "/" + pic.vPartTotal.ToString() + " parts collected";
    }*/
}